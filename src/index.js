import React from 'react';
import ReactDOM from 'react-dom';
import TodoApp from './TodoApp'; // 親コンポーネントのインポート
import './index.css'; // スタイルシートのインポート（必要に応じて）

ReactDOM.render(<TodoApp />, document.getElementById('root'));